/**
 * Icon配置
 * 
 * 根据项目需要进行配置
 */
const iconConfig = {
  /**
   * icon 生成位置
   * ⚠️注意：结尾请加上'/'
   */
  path: './',

  /**
   * icon 统一导出路径
   * 若不需导出，请设置为空
   */
  exportPath: '',

  /** 生成扩展名 */
  defaultExtension: '.js',

  /** 文件名长度限制 */
  fileNameLength: 20
};

export {
  iconConfig
};